# Maintainer: JuckZ <juckz@foxmail.com>
# Template only. Do not submit until binary redistribution is cleared and
# REPLACE_WITH_SHA256 is replaced with the release artifact hash.

pkgname=codex-desktop-bin
pkgver=2026.06.16.063729
pkgrel=1
pkgdesc="Codex Desktop for Linux, prebuilt from the upstream macOS DMG conversion flow"
arch=('x86_64')
url="https://github.com/JuckZ/codex-desktop-bin"
license=('MIT' 'custom')
depends=(
  'python'
  'p7zip'
  'polkit'
  'curl'
  'unzip'
  'gcc'
  'make'
  'alsa-lib'
  'at-spi2-core'
  'atk'
  'cairo'
  'dbus'
  'glib2'
  'gtk3'
  'libcups'
  'libdrm'
  'libx11'
  'libxcb'
  'libxcomposite'
  'libxdamage'
  'libxext'
  'libxfixes'
  'libxkbcommon'
  'libxrandr'
  'mesa'
  'nspr'
  'nss'
  'pango'
)
optdepends=(
  'nodejs>=22.22.0: optional override for the bundled managed Node.js runtime'
  'npm: optional override for Codex CLI install/update flows'
  'zenity: GTK dialog fallback when the Codex CLI is missing'
  'kdialog: KDE dialog fallback when the Codex CLI is missing'
)
provides=('codex-desktop')
conflicts=('codex-desktop' 'openai-codex-desktop')
source=("${pkgname}-${pkgver}-${pkgrel}-x86_64.pkg.tar.zst::${url}/releases/download/v${pkgver}/${pkgname}-${pkgver}-${pkgrel}-x86_64.pkg.tar.zst")
sha256sums=('REPLACE_WITH_SHA256')

package() {
  cp -a "${srcdir}/opt" "${pkgdir}/"
  cp -a "${srcdir}/usr" "${pkgdir}/"
}
